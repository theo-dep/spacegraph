# SpaceTree

C++ header-only spatial node implementation designed for any 3D applications which have matrix 4x4 transformations.

## Build tests with CMake and vcpkg

```
cmake --preset=vcpkg
cmake --build --preset=vcpkg
ctest --preset=vcpkg
```
